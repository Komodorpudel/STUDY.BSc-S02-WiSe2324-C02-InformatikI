#include <stdio.h>

int main () {

    double myDouble = DBL_MAX;

    printf("DBL_MAX: %f\n");

    printf("myDouble: %f", myDouble + 1);

    return 0;

    // DBL MAX ist der maximale double wert
}